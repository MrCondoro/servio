rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // MVP: open access for presentation — tighten before production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
